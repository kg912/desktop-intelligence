/**
 * Compile-time constants injected by Rollup define in electron.vite.config.ts.
 * These are replaced with literal values (true/false) at build time and are
 * never present as runtime variables — do NOT reference them via process.env.
 */
declare const __DEV_MODE__: boolean
